//! `<svelte:element>` analyze pass — no direct upstream equivalent
//! (Element.ts handles both static and dynamic).
//!
//! Currently empty; `visit_svelte_element` migrates from
//! `template_walker.rs` per Step 2 of
//! `notes/TEMPLATE_WALKER_SPLIT.md`.
